import {
  ArgTable,
  Meta,
  Canvas,
  Preview,
  Story,
} from "@storybook/addon-docs/blocks";
import { config, withDesign } from "storybook-addon-designs";
import item1Img from "../../assets/item1.png";
import Card from ".";
import Button from "../Button";
import Icon from "../Icon";


<Meta title="Components/Card" parameters={{
    viewport: {
      defaultViewport: 'mobile1'
    },
    design: config({
        type: "figma",
        url:
          "https://www.figma.com/file/AWWkZAdBsBvZJHu6PnjbtP/Workshop-design-file-1?node-id=264%3A397",
      }),
  }}/>

export const cardArgs = {
  name: "Nike Air Force 1 '07 LX",
  image: item1Img,
  price: "95.00",
  subtitle: "Men's Shoe",
};

## Card

Card component displays product name, image and price.

<Preview>
  <Story name="Small" args={cardArgs} >
    {(args) => <Card size="small" {...args} />}
  </Story>
  <Story name="Medium" args={cardArgs}>
    {(args) => <div style={{maxWidth: "140px"}}>
<Card size="medium" {...args} />
</div>
}
  </Story>
  <Story name="Big" args={cardArgs}>
    {(args) => <div style={{maxWidth: "295px"}}><Card size="big" {...args} /></div>}
  </Story>
</Preview>
